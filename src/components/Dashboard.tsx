import { ComingUp } from './ComingUp'

// Placeholder — replaced by the full dashboard in Task 1.3.
export function Dashboard({ caseId: _caseId }: { caseId: string }) {
  return <ComingUp title="Trial-readiness dashboard" blurb="Gauge, counters, risk register and verdict — built next." />
}
